<!DOCTYPE html>
<html lang=ja>
<head>
<meta charset="UTF-8">
</head>
<body>
    <table>
        <% for( let result of content ) { %>
        <tr>
            <td><%= result.id %></td>
            <td><%= result['都道府県'] %></td>
            <td><%= result['population'] %></td>
        </tr>
        <% } %>
    </table>
    <form action="/" method="get">
        並び順：<select name="sorting">
            <option value="人口">人口</option>
            <option value="男性">男性</option>
            <option value="女性">女性</option>
            <option value="国立大学/大学*100">国立割合</option>
            <option value="公立大学/大学*100">公立割合</option>
            <option value="私立大学/大学*100">私立割合</option>
        </select>
        表示数：<input type="text" name="number" size="10">
        <input type="submit" value="並び替える">
    </form>
</body>
</html>
